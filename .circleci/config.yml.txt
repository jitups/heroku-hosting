version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout 

     # build image
     - run: |         
         docker info
         docker build -t aspnetapp -f Dockerfile .

     # deploy the image
     - run: |         
         docker login --username=jitusurve@gmail.com --password=SURjitP@1 registry.heroku.com
         docker tag aspnetapp registry.heroku.com/test-app-jitu/web
         docker push registry.heroku.com/test-app-jitu/web                
         curl https://cli-assets.heroku.com/install.sh | sh
         heroku container:release web -a test-app-jitu